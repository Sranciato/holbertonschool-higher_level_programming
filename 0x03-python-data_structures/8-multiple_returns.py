#!/usr/bin/python3
def multiple_returns(sentence):

    i = 0
    for i in range(len(sentence)):
        continue
    if not sentence[:1]:
        char = None
        i = -1
    else:
        char = sentence[:1]
    tuple = (i + 1, char)
    return tuple
